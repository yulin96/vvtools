# 更新日志

## 未发布

- 修复 Homebrew 6 无法解析 Cask 导致 macOS 版本无法安装的问题。

## v0.0.15

- macOS 版本改用 Ad-hoc 签名并支持应用内下载和重启安装更新；发布流程会按 ARM64/x64 生成并校验 `latest-mac.yml`，自动更新失败时可转到 GitHub 手动下载。
- macOS 新增 Homebrew Cask 安装和升级方式，安装后会自动清除应用隔离属性；Cask 通过发布流程生成的稳定下载别名跟随最新版本，无需逐版本维护校验值。

## v0.0.14

- 字体任务支持并行批量处理：自动调度会根据可用逻辑处理器分配最多 4 个字体任务，自定义模式也可选择同时处理 1～4 个字体。
- 设置页的处理组件状态改为响应式卡片网格，缩短纵向列表并集中展示组件名称、版本和可用状态。

## v0.0.13

- 字体处理新增快速转换：拖入 TTF 或 OTF 时自动生成原格式、WOFF 和 WOFF2，拖入 WOFF 时自动生成 WOFF 和 WOFF2，所有任务默认使用中文 8105 字符范围压缩。
- 所有处理页面的任务表移除常驻“节省”列，压缩收益改为仅在产生已完成任务后显示于批次顶部汇总，并为状态及任务参数留出更多空间。

## v0.0.12

- 整个应用窗口（包括设置页和侧栏）均可拖入媒体文件，并会根据文件类型自动切换到对应的图片、视频、音频、PDF 或字体处理页后继续加入待处理列表。
- 错误提示改为内容区右上角的浮层通知，不再挤压处理设置和待处理列表的页面布局。
- 移除媒体处理内容区的外层边框和配置面板阴影，配置区与侧栏顶部对齐，媒体列表及设置页内容在接近上方边界时平滑淡出；浅色模式及 Windows 原生窗口控制区统一为白色，拖入文件时保留全窗口光圈和更清晰的点阵反馈，并在拖拽期间隐藏内容滚动条。

## v0.0.11

- 内置 FFmpeg 和 FFprobe 升级至 8.1.2，安装及发布时按平台下载固定版本并校验完整性，提升新格式兼容性和转换稳定性。
- Windows 新增 AMD AMF 硬件编码检测与 H.264/H.265 转换支持，自动和硬件模式现在可使用兼容的 AMD 显卡加速。
- 左侧导航改为不可拖动的按钮，并在全局取消可点击控件的手型光标；侧栏悬停时也不再显示链接地址浮层。

## v0.0.10

- 视频自定义帧率和最大高度输入框改为在分段选择内按需展开，选择“自定义”后直接输入并自动聚焦。
- PDF 转图片改为每个 PDF 只创建一个任务，并把所有分页图片统一输出到以 PDF 命名的独立文件夹。

## v0.0.9

- 字体转换任务新增逐项压缩范围，可直接选择不压缩、西文基础及中文 3500、6500、8105，并调整任务表列宽避免输出格式被遮挡。
- 优化批量任务性能：进度更新改为限频的单任务增量通知，避免反复克隆和传输整批任务。
- PDF 预检与处理移至独立子进程，连续导出同一 PDF 的多个页面时会复用已加载文档，减少主界面卡顿和重复读取解析。
- PDFium 改为首次 PDF 预检或处理时再初始化，减少未使用 PDF 功能时的启动工作和初始内存占用。
- 图片转换结果大于源文件时不再保存或覆盖文件，任务列表会以醒目的“已跳过”状态标识并显示增大的体积。
- 图片自动与自定义并发上限提高到 16，并扩展 Sharp 并行任务线程池、限制单张图片线程数，以更充分利用多核处理器并避免线程过度抢占。
- 图片加入待处理列表后会以有限并发在后台读取原始文件大小和尺寸，并逐项显示读取结果。
- 压缩 Windows 和 macOS 顶部标题栏高度，并在拖动区域显示当前模块与处理中任务数量，减少顶部空白。
- 将软件更新移至设置页顶部，新增 GitHub 开源地址入口，并修复处理组件状态与重新检测按钮被挤压换行的问题。
- 默认文件名后缀改为 `_c`，并移除任务完成通知、提示音和完成后自动打开输出位置功能。
- 修复 Windows 右上角原生窗口控制区与应用标题栏背景颜色不一致的问题，并使其跟随浅色和深色主题切换。

## v0.0.8

- 字体压缩新增西文基础、常用中文 3500、通用中文 6500 和完整规范中文 8105 字符预设；支持补充字符，以及自定义文本或 TXT 文件并按需保留西文基础。
- 新增 PDF 处理与字体处理：PDF 无损/有损压缩、逐页导出 PNG/JPEG/WebP，以及 TTF、OTF、WOFF、WOFF2 转换、TTC/OTC 集合拆分、可变字体静态化和字体子集化；PDF 与字体处理方式移至设置区顶部，便于直接切换。
- 修复普通字体转换被空实例信息阻止的问题；字体待处理列表新增逐项输出格式，同一字体可重复添加并同时转换为不同格式。
- 修复 WOFF2 转换期间字体压缩计算阻塞应用界面的问题，并消除字体 `meta` 元数据表被丢弃时的无关警告。
- 修复字体压缩缺少保留字符时显示底层 IPC 错误的问题，改为在输入框旁提示并自动聚焦待补充内容。
- 修复字体任务已经开始却持续显示“等待中 0%”的问题；取消任务后会清空进度条，不再继续播放进度动画。
- 统一图片、视频、音频、PDF 和字体页面的配置栏：输出位置与开始处理移回面板首行，PDF/字体处理方式使用统一的紧凑切换样式，空状态说明会跟随处理方式变化；设置文件名后缀后不再提示将替换源文件。
- 修复部分交叉引用表不规范的 PDF 已成功压缩，却因 qpdf 结构修复警告而被误报失败的问题。
- 优化图片、视频、音频及通用设置中的参数名称，改用更直观的输出结果和操作描述，明确文件夹层级、输出位置与源文件替换行为，并将更多设置统一为紧凑的单排布局。
- 视频输出帧率和最大分辨率改为分段切换，支持输入自定义帧率和最大高度；精简帧率选项，并限制紧凑分段选择最多显示五项。
- 视频新增 AVI 输出与 MPEG-4 编码，将输出格式和视频编码改为逐行布局，并把编码加速移入三列更多设置。
- 移除图片与视频的可编辑预设配置，将顶部预设改为固定分段切换：视频提供自定义、低质量、中质量和高质量，图片提供自定义、网站图片和分享图。
- 修复收起或展开左侧栏后分段选项的选中背景宽度和位置未及时更新的问题。

## v0.0.7

- 优化高级设置展开与收起动效，移除模糊效果以提升流畅度。
- 调整主窗口默认尺寸，减少首次打开时的屏幕占用。
- 记住主窗口尺寸、位置和最大化状态，并在显示器变化后安全恢复窗口。
- 记住上次使用的媒体页面。
- 高级设置每次进入页面时默认收起，并在点击文件或任务内容区时自动收起。
- 简化文件拖入反馈，移除指针后方的旋转与光圈装饰，并增强背景点阵。
- 移除覆盖源文件前的二次确认弹窗，处理开始后仍仅在成功时替换源文件。
- 将原目录覆盖提示移至主操作区，在高级设置收起时也保持可见，并增加平滑出现与消失过渡。
- 将图片元数据保留策略改为三段切换，便于直接查看和调整当前模式。
- 重构设置架构：全局设置仅保留通用规则，图片和视频预设移至各自页面的侧边设置抽屉；新增按媒体类型自动或自定义的任务并发，并将图片预设精简为三组常用方案及五类核心参数。
- 图片和视频页面设置采用无模糊的侧边抽屉，支持遮罩、Esc 和焦点管理，打开与关闭更平滑。
- macOS 和 Windows 使用类似新版 VS Code 的紧凑顶部标题栏，将侧栏切换与媒体页主操作移至顶部，主题切换仍保留在侧栏；移除顶部标题文字，增加操作区安全留白，并将标题栏控件统一为 32px 高度，同时按内容区顶部留白校正 macOS 操作区的视觉居中。保留系统原生控制按钮，并将窗口外边距与主区域间距统一收紧为 6px；侧栏展开与折叠时保持一致的导航顶部间距。Linux 继续使用原生标题栏，不再维护自定义窗口控制区。
- 图片页恢复“更多参数”展开区，用于调整元数据、目录结构和较小图片策略；页面设置抽屉改为全窗口蒙版，并支持点击蒙版关闭。
- 媒体页面顶部操作保持单行，页面设置入口收为图标按钮；增强设置抽屉的圆角、表面和阴影层级。
- 任务关闭行为改为独立的分段切换，图片自动并发上限从 4 个提高到 8 个。

## v0.0.6

## v0.0.5

- 优化 Windows 窗口标题栏布局，使用原生窗口控制按钮并减少顶部空白占用。

## v0.0.4

## v0.0.3

- 修复 Windows 下图片处理后文件句柄未及时释放，可能导致源文件替换或临时文件清理失败的问题。

## v0.0.2

## v0.0.1

- 新增应用内版本信息、更新日志、自动检查更新、下载进度与重启安装入口。

## v0.0.0

- 项目开发版本。
